Put your resume here as **resume.pdf** (exact name, lowercase).

Anything inside this `public/` folder is served from the site root, so
`react-app/public/resume.pdf`  →  is downloaded by the "Download Resume" button via `/resume.pdf`.

To use a different filename, update the href in `src/components/Hero.jsx`.
